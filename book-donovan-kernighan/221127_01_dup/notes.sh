# an example of how to run the program

# before running it we need to compile the program with go build
# then we use the example text
# the dup1 program will check if there are any duplicates in the text
cat a-text.txt | ./dup1
